import styled from 'styled-components';

export const Wrapper = styled.nav`
    ul {
        display: flex;
        justify-content: space-evenly;
        list-style: none;
        padding: 0;
    }
`